/**
 * The PostCSS config used to
 * configure PostCSS for the app
 */
const postcssConfig = {
  plugins: {
    // eslint-disable-next-line @typescript-eslint/naming-convention
    '@tailwindcss/postcss': {},
  },
};

export default postcssConfig;
